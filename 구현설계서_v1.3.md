# AI 버블 논쟁 모니터 — 구현 설계서 v1.3

대상: 코딩 에이전트 · 선행 문서: 서비스 명세서 v1.0, PoC(`poc/`)
언어: Python 3.11+ · 상태: 착수 가능
v1.0·v1.1·v1.2 대체본. 이전 버전과 충돌하면 **이 문서가 이긴다.** 이전 버전은 참조하지 말 것.

v1.2에서 추가된 것: 화면 설계 원칙(§19), 라우트와 화면 명세(§20), 빈 상태 규정(§21).

---

## 0. 에이전트를 위한 지침

이 문서는 순서대로 실행하도록 작성됐다. §14의 작업 목록(T1~T24)이 실제 작업 단위이고, 나머지 절은 그 작업들이 참조하는 명세다.

**작업 시작 전 반드시 §1을 읽을 것.** §1의 불변 규칙은 성능이나 편의를 이유로 위반할 수 없다. 규칙과 충돌하는 요구가 생기면 구현하지 말고 사람에게 물어라.

**추측한 API 스펙으로 코드를 쓰지 말 것.** `[확인필요]` 표시가 붙은 항목은 실제 응답을 한 번 받아보고 스키마를 확정한 뒤 구현한다.

**숫자를 채워 넣지 말 것.** 이 문서에서 `null`이나 "사람이 결정"으로 남은 값 — 특히 판정선(§8) — 은 에이전트가 정하지 않는다. §8의 절차를 실행해 산출물을 사람에게 제출하는 것까지가 작업이다.

**작업 매핑의 진실은 §14 하나다.** 다른 절에 T번호를 적지 않는다.

---

## 1. 불변 규칙

위반 시 기능이 아니라 제품이 망가진다.

| # | 규칙 | 코드 수준의 강제 |
|---|---|---|
| R1 | 복수 지표를 하나의 종합 점수·게이지·0~100 지수로 합성하지 않는다. 단일 지표 내부의 집계(HHI, 상관계수 등)는 허용한다 | 지표 함수는 다른 지표의 결과를 입력으로 받을 수 없다. 예외는 화이트리스트 등록 파생지표뿐이며(§6.0) 파생지표는 규칙에 연결할 수 없다 |
| R2 | 매수/매도/비중 관련 출력을 하지 않는다 | 출력 문자열에 매매 어휘 금지 |
| R3 | `undecidable` 전제는 어떤 규칙도 변경할 수 없다 | 판정 엔진에 가드. 테스트로 고정 |
| R4 | 전제는 `source_doc` 없이 등록 불가 | 스키마 검증에서 거부 |
| R5 | 원장(ledger)은 append-only. `observation`도 append-only이며 정정은 리비전으로 남긴다 | UPDATE/DELETE 트리거로 차단 |
| R6 | 지표가 없으면 `null`로 두고 미관측으로 표시한다. 추정치로 채우지 않는다 | 결측 보간 함수 작성 금지 |
| R7 | 비활성 지표는 어떤 전제도 `true`/`false`로 만들 수 없다. 만료 규칙(R10)에 의한 `undetermined` 복귀만 예외 | `available=False`면 판정 규칙 호출을 건너뜀. 만료 규칙은 별도 경로 |
| R8 | 상태가 안 바뀌면 아무것도 기록하지 않는다 | 변경 감지 후에만 insert |
| R9 | LLM에 전제를 생성시키지 않는다 | LLM 호출은 후보 추출까지만. 확정은 사람 |
| R10 | 근거 지표가 연속 N기간 미관측이면 전제는 `undetermined`로 돌아간다 | 만료 규칙. N은 `thresholds.yaml`. `undecidable`에는 적용 안 함(R3 우선) |
| R11 | 규칙은 `IndicatorResult.value`만 읽는다. payload 접근 금지, DB 접근 금지, 부작용 금지 | 엔진이 `RuleContext`만 주입 |
| R12 | 재생 시 `as_of` 이후에 공개된 정보를 쓰지 않는다 | 모든 사실 조회에 `filed <= as_of` 조건. 코호트도 `as_of` 기준 구성. 위반 시 예외 |
| R13 | 판정선의 기준점(anchor)은 전제 문장에서 유도한다. 백필 결과를 보고 정하지 않는다 | `anchor_method='semantic'`과 `anchor_rationale`이 없으면 규칙 활성화 거부 |

---

## 2. 시스템 개요

```
수집기(collectors)  ->  지표(indicators)  ->  판정 규칙(rules)  ->  원장(ledger)
      |                       |                                        |
   raw 저장          observation 저장(리비전)                        변경분만
      |                       |                                        |
      +------------- 나우캐스트 오차 기록 --------------+          리포트/API
```

핵심: **원장이 상품이다.** 나머지는 원장을 채우기 위한 배관이다.

따름정리 둘.

1. **원장만 읽어도 판정 근거가 재구성돼야 한다.** 원장이 외부 테이블을 가리키기만 하고 그 테이블이 나중에 바뀌면 원장은 상품이 아니다. `ledger.evidence`에 판정 시점의 관측값 스냅샷을 담는 이유다(§4.1).
2. **원장은 언제 알 수 있었는지를 기록한다.** 실시간으로 포착한 전이와 사후에 재구성한 전이를 구분하지 못하면 원장은 자기가 예언자인 척하게 된다. 시간축을 둘로 나누는 이유다(§4.1, §9).

---

## 3. 리포지토리 구조

```
bubble-monitor/
  pyproject.toml
  .env.example
  README.md
  src/bm/
    __init__.py
    config.py            설정, 환경변수, 코호트 로딩
    db.py                SQLite 연결, 마이그레이션, append-only 트리거
    models.py            dataclass + 스키마 검증
    http.py              공통 HTTP 클라이언트 (레이트리밋, 재시도, 캐시, offline)
    clock.py             재생 시계. as_of 주입과 룩어헤드 가드 (R12)
    collectors/
      __init__.py        레지스트리
      base.py            Collector 프로토콜
      sec_xbrl.py
      sec_fts.py
      kcs_trade.py       관세청
      twse.py
      fred.py
    indicators/
      __init__.py        레지스트리 + 파생지표 화이트리스트
      self_funding.py    hyper / neo / gap
      offbalance.py      비활성 상태로 시작
      supply_chain.py
      commitment.py
    rules/
      __init__.py        레지스트리
      engine.py          판정 엔진, 가드, 만료 규칙, 이력현상
      definitions.py     규칙 구현
    backfill.py          시간순 재생 오케스트레이터
    thresholds.py        판정선 로딩·검증·buffer 산출
    ledger.py            원장 읽기/쓰기, run 관리
    nowcast.py           예측 오차 기록
    report.py            콘솔 리포트
    doctor.py            설정·키·태그 가용성 점검
    api.py               읽기 전용 JSON API
    cli.py               엔트리포인트
  data/
    premises.yaml        전제 원장 (사람이 편집)
    sources.yaml         출처 문서 레지스트리
    cohorts.yaml         코호트 구성 이력 (편입·제외 날짜 필수)
    thresholds.yaml      판정선, 완충폭, 만료 기간, 각종 임계
    queries.yaml         EDGAR 전문검색어
    hs_codes.yaml        관세청 HS 10단위 코드
  tests/
    fixtures/            녹화된 API 응답
    test_*.py
  migrations/
    001_init.sql
```

---

## 4. 데이터 모델

### 4.1 SQLite 스키마 (`migrations/001_init.sql`)

```sql
-- 관측값. append-only. 정정은 새 리비전으로 쌓는다. R5
CREATE TABLE observation (
  id            INTEGER PRIMARY KEY,
  indicator_id  TEXT NOT NULL,
  period        TEXT NOT NULL,        -- 'YYYY-MM-DD' 기간 종료일
  revision      INTEGER NOT NULL,     -- 1부터. 정정 시 증가
  value         REAL,                 -- NULL 허용 = 미관측
  unit          TEXT,
  available     INTEGER NOT NULL,     -- 0/1
  reason        TEXT,                 -- available=0일 때 필수
  payload       TEXT,                 -- 원본 JSON
  collected_at  TEXT NOT NULL,
  known_at      TEXT NOT NULL,        -- 이 값이 공개된 시점 (SEC filed 등). R12
  source_url    TEXT,
  superseded_at TEXT,
  UNIQUE(indicator_id, period, revision),
  CHECK(available = 1 OR reason IS NOT NULL)
);

CREATE TRIGGER observation_immutable BEFORE UPDATE ON observation
WHEN old.indicator_id IS NOT new.indicator_id
  OR old.period       IS NOT new.period
  OR old.revision     IS NOT new.revision
  OR old.value        IS NOT new.value
  OR old.unit         IS NOT new.unit
  OR old.available    IS NOT new.available
  OR old.reason       IS NOT new.reason
  OR old.payload      IS NOT new.payload
  OR old.collected_at IS NOT new.collected_at
  OR old.known_at     IS NOT new.known_at
  OR old.source_url   IS NOT new.source_url
BEGIN SELECT RAISE(ABORT,
  'observation is append-only; only superseded_at may change'); END;

CREATE TRIGGER observation_no_delete BEFORE DELETE ON observation
BEGIN SELECT RAISE(ABORT, 'observation is append-only'); END;

CREATE VIEW observation_current AS
SELECT * FROM observation WHERE superseded_at IS NULL;

-- 전제. premises.yaml에서 동기화. status는 여기가 진실.
CREATE TABLE premise (
  id            TEXT PRIMARY KEY,
  side          TEXT NOT NULL CHECK(side IN ('bubble','counter','shared')),
  crux          TEXT,
  claim         TEXT NOT NULL,
  source_doc    TEXT NOT NULL,        -- R4
  source_quote  TEXT NOT NULL,
  indicator_id  TEXT,                 -- NULL = 미관측
  status        TEXT NOT NULL CHECK(status IN
                  ('true','false','undetermined','undecidable')),
  note          TEXT,
  archived_at   TEXT,
  updated_at    TEXT NOT NULL
);

-- 재생 런. 원장 행은 지우지 않고 런을 superseded로 표시한다.
CREATE TABLE ledger_run (
  run_id           TEXT PRIMARY KEY,
  started_at       TEXT NOT NULL,
  finished_at      TEXT,
  window_from      TEXT NOT NULL,
  window_to        TEXT NOT NULL,
  code_version     TEXT NOT NULL,     -- git sha
  thresholds_hash  TEXT NOT NULL,     -- thresholds.yaml 내용 해시
  status           TEXT NOT NULL CHECK(status IN ('active','superseded')),
  superseded_by    TEXT,
  supersede_reason TEXT,
  CHECK(status = 'active' OR (superseded_by IS NOT NULL
                              AND supersede_reason IS NOT NULL))
);

-- 원장. append-only. R5
CREATE TABLE ledger (
  id            INTEGER PRIMARY KEY,
  as_of         TEXT NOT NULL,        -- 판정 근거가 성립한 시점 = 공시일
  recorded_at   TEXT NOT NULL,        -- 물리적 기록 시각
  origin        TEXT NOT NULL CHECK(origin IN ('live','replay')),
  run_id        TEXT,                 -- origin='replay'면 필수
  premise_id    TEXT NOT NULL,
  crux          TEXT,
  claim         TEXT NOT NULL,
  status_from   TEXT NOT NULL,
  status_to     TEXT NOT NULL,
  reason        TEXT NOT NULL,
  indicator_id  TEXT,
  observation_id INTEGER,             -- 편의용 링크. 근거의 원본이 아니다
  evidence      TEXT NOT NULL,        -- JSON. §4.2 형식 필수
  actor         TEXT NOT NULL,        -- 'rule:<id>' | 'human:<id>'
  CHECK(origin = 'live' OR run_id IS NOT NULL)
);

CREATE TRIGGER ledger_no_update BEFORE UPDATE ON ledger
BEGIN SELECT RAISE(ABORT, 'ledger is append-only'); END;
CREATE TRIGGER ledger_no_delete BEFORE DELETE ON ledger
BEGIN SELECT RAISE(ABORT, 'ledger is append-only'); END;

-- 유효 원장. 리포트·API는 이것만 읽는다.
CREATE VIEW ledger_effective AS
SELECT l.* FROM ledger l
LEFT JOIN ledger_run r ON l.run_id = r.run_id
WHERE l.origin = 'live' OR r.status = 'active';

-- 나우캐스트 오차. 예측값은 절대 observation에 들어가지 않는다. 재생 불가.
CREATE TABLE nowcast_error (
  id            INTEGER PRIMARY KEY,
  indicator_id  TEXT NOT NULL,
  target_period TEXT NOT NULL,
  predicted     REAL NOT NULL,
  predicted_at  TEXT NOT NULL,
  actual        REAL,
  actual_at     TEXT,
  abs_error     REAL,
  days_ahead    INTEGER,
  UNIQUE(indicator_id, target_period, predicted_at)
);

-- 이벤트 후보. LLM/정규식이 뽑고 사람이 확정. R9
CREATE TABLE event_candidate (
  id            INTEGER PRIMARY KEY,
  found_at      TEXT NOT NULL,
  source_url    TEXT NOT NULL,
  form_type     TEXT,
  filer         TEXT,
  filed_at      TEXT,
  excerpt       TEXT NOT NULL,
  excerpt_hash  TEXT NOT NULL,        -- sha256(공백정규화+소문자화)
  suggested_kind TEXT,                -- 'guarantee'|'commitment'|'investment'
                                      -- |'lease'|'restatement_review'
  review_status TEXT NOT NULL DEFAULT 'pending'
                  CHECK(review_status IN ('pending','confirmed','rejected')),
  reviewed_by   TEXT,
  reviewed_at   TEXT,
  UNIQUE(source_url, excerpt_hash)
);

-- 약정 원장. 행 하나 = 약정 1건. 한 문서에서 여러 행이 나올 수 있다.
CREATE TABLE commitment (
  id            INTEGER PRIMARY KEY,
  doc_id        TEXT NOT NULL,
  doc_url       TEXT NOT NULL,
  line_no       INTEGER NOT NULL,
  filed_at      TEXT NOT NULL,
  counterparty  TEXT NOT NULL,
  obligor       TEXT NOT NULL,
  amount_usd    REAL,
  kind          TEXT NOT NULL,
  dup_group     TEXT,
  dup_status    TEXT NOT NULL DEFAULT 'unchecked'
                  CHECK(dup_status IN ('unchecked','unique','duplicate')),
  UNIQUE(doc_id, line_no)
);

CREATE INDEX idx_obs ON observation(indicator_id, period, revision);
CREATE INDEX idx_obs_known ON observation(indicator_id, known_at);
CREATE INDEX idx_ledger_premise ON ledger(premise_id, as_of);
```

**`known_at`이 재생의 핵심이다.** 이 컬럼이 없으면 R12를 지킬 수 없다. SEC 사실은 `filed`를, 다른 소스는 수집 시각을 넣는다(그래서 다른 소스는 재생 불가다 — §9.2).

### 4.2 `ledger.evidence` 형식

이 형태가 아니면 insert를 거부한다.

```json
[
  {
    "doc_id": "IFC-2026-08-24",
    "url": "https://...",
    "quote": "인용 원문",
    "observed": {
      "indicator_id": "self_funding_hyper",
      "period": "2026-06-30",
      "revision": 1,
      "value": 0.042,
      "unit": "ratio",
      "known_at": "2026-07-29",
      "tag_used": "PaymentsToAcquirePropertyPlantAndEquipment"
    }
  }
]
```

`observed`는 규칙이 만든 원장 행에서 필수다. 사람 판정(`actor='human:*'`)은 생략할 수 있으나 `doc_id`와 `quote`는 필수다.

### 4.3 `premises.yaml` ↔ DB 동기화 규칙

- `claim`, `source_doc`, `source_quote`, `side`, `crux`, `indicator_id`는 **YAML이 진실**. 기동 시 DB에 upsert
- `status`는 **DB가 진실**. YAML의 status는 최초 1회 시드로만 사용
- YAML에서 전제를 삭제해도 DB에서 지우지 않는다. `archived_at`을 찍고 리포트·API에서만 제외
- 기동 시 검증: 모든 `@rule`의 `premises`가 YAML에 존재하고, 각 전제의 `indicator_id`가 규칙의 `indicator`와 일치할 것. 불일치는 **기동 실패**

```yaml
# data/premises.yaml
- id: P-A-03
  side: bubble
  crux: 조달능력
  claim: 하이퍼스케일러는 영업현금흐름만으로 CAPEX를 감당하지 못한다
  source_doc: IFC-2026-08-24
  source_quote: "..."
  indicator_id: self_funding_hyper
  status: undetermined
```

### 4.4 `cohorts.yaml`

재생에서 생존편향을 막으려면 구성 이력이 날짜와 함께 있어야 한다(R12).

```yaml
hyperscaler:
  - ticker: MSFT
    from: 2020-01-01
    to: null
  - ticker: XYZ
    from: 2020-01-01
    to: 2024-06-30        # 편입 해제
neocloud:
  - ticker: ...
```

코호트 조회는 항상 `as_of`를 받는다. `from <= as_of AND (to IS NULL OR as_of < to)`.

---

## 5. 수집기 명세

### 5.0 공통 (`http.py`)

```python
class Client:
    def get_json(self, url: str, *, params: dict | None = None,
                 headers: dict | None = None, cache_ttl: int = 3600) -> dict | None: ...
```

- 도메인별 레이트리밋. SEC는 **10 req/s 상한, 실제로는 8로 설정**
- 지수 백오프 재시도: 429/500/502/503/504에 대해 최대 5회, base 1s
- 404는 재시도하지 않고 `None` 반환 (태그 미사용 = 정상 신호)
- 응답 디스크 캐시(`.cache/`), 키는 URL+params 해시. `--no-cache`로 무시
- **`--offline`**: 네트워크 호출을 아예 하지 않는다. 캐시와 `tests/fixtures/`만 사용하고, 미스는 예외가 아니라 `available=False, reason='offline_miss'`로 처리. offline 모드에서 소켓이 열리면 테스트 실패
- 모든 요청 로깅: URL, 상태코드, 소요시간, 캐시 히트 여부

### 5.1 SEC XBRL (`collectors/sec_xbrl.py`)

**엔드포인트**

| 용도 | URL |
|---|---|
| 티커→CIK | `https://www.sec.gov/files/company_tickers.json` |
| 개념 조회 | `https://data.sec.gov/api/xbrl/companyconcept/CIK{cik10}/us-gaap/{tag}.json` |
| 전체 사실 | `https://data.sec.gov/api/xbrl/companyfacts/CIK{cik10}.json` |
| 기간 횡단 (duration) | `https://data.sec.gov/api/xbrl/frames/us-gaap/{tag}/USD/CY{YYYY}Q{Q}.json` |
| 기간 횡단 (instant) | `https://data.sec.gov/api/xbrl/frames/us-gaap/{tag}/USD/CY{YYYY}Q{Q}I.json` |
| 제출 목록 | `https://data.sec.gov/submissions/CIK{cik10}.json` |

**`I` 접미사 주의.** `I`는 시점(instant) 사실 전용이다. capex·OCF·매출은 duration이므로 `I` 없는 URL을 쓴다. 잘못 쓰면 빈 응답이 온다. 보증 잔액 같은 잔액 태그에만 `I`를 붙인다.

**필수 헤더**: `User-Agent: <앱명>/<버전> <연락 이메일>`. 없으면 403.

**[중요] `filed` 필드를 반드시 보존한다.** `companyconcept`/`companyfacts`의 각 사실에는 공시일이 붙어 있다. 이 값이 `observation.known_at`이 되고, 재생에서 룩어헤드를 막는 유일한 수단이다(R12). 버리지 말 것.

**[중요] 현금흐름 항목은 YTD 누적인 경우가 많다.** 다만 분기값을 직접 태깅하는 회사도 상당수다. 둘 다 있을 수 있으므로 우선순위를 지킨다.

```python
def to_quarterly(facts: list[dict], as_of: date | None = None
                 ) -> dict[str, QuarterValue]:
    """
    us-gaap duration 사실 목록을 분기 값으로 변환.

    0. as_of가 주어지면 filed <= as_of 인 사실만 남긴다 (R12).
       재생에서 이 필터가 빠지면 수정 공시가 미래에서 새어 들어온다.
    1. (start, end)로 그룹. 동일 (start,end)에 값이 여럿이면
       가장 최근 filed 를 채택 (0단계 필터 이후 기준)
    2. 기간 길이로 분류: Q(80~100일), H(170~190), 9M(260~285), FY(350~380)
    3. 채택 우선순위:
       a. 직접 태깅된 Q가 있으면 그것을 쓴다.        derivation='direct'
       b. 없으면 같은 회계연도 안에서 누적을 차분한다. derivation='diff'
            Q2 = H - Q1, Q3 = 9M - H, Q4 = FY - 9M
       c. 둘 다 있고 |direct - diff| / |direct| > 0.02 이면
          payload.qdiff_mismatch 에 두 값을 남기고 direct 채택, 리포트 표시
    4. 차분 결과가 음수이고 원래 지표가 양수여야 하면 그 분기는 버리고
       reason='ytd_diff_negative' 로 관측 결측 처리 (R6: 추정 금지)
    5. payload.derivation 과 채택 사실의 filed 를 항상 기록한다
    """
```

**회계연도 정렬**: 코호트 합산 전에 종료일을 달력 분기로 버킷팅한다. 종료일이 분기말 ±45일 이내면 해당 달력 분기에 배정하고, **정확히 45일이면 이른 분기에 배정**한다. 엔비디아(1월말 종료), 브로드컴(10월말 종료)이 이 처리 없이는 어긋난다. 마이크로소프트는 회계연도 라벨만 다르고 분기 종료일은 달력 분기와 같으므로 버킷팅에는 영향이 없으나, FY 라벨을 그대로 쓰면 정렬이 깨지므로 **라벨이 아니라 종료일로만 배정한다.** 배정 결과는 `payload`에 원본 종료일과 함께 남긴다.

**대상 태그**

```python
TAGS_SELF_FUNDING = {
    "capex": ["PaymentsToAcquirePropertyPlantAndEquipment",
              "PaymentsToAcquireProductiveAssets"],   # 폴백 순서
    "ocf":   ["NetCashProvidedByUsedInOperatingActivities",
              "NetCashProvidedByUsedInOperatingActivitiesContinuingOperations"],
}
TAGS_OFFBALANCE = [
    "GuaranteeObligationsMaximumExposure",
    "GuaranteeObligationsCurrentCarryingValue",
    "LesseeOperatingLeaseLeaseNotYetCommencedPaymentsDue",
    "LesseeFinanceLeaseLeaseNotYetCommencedPaymentsDue",
    "LesseeOperatingLeaseLeaseNotYetCommencedAmount",
    "UnrecordedUnconditionalPurchaseObligationBalanceSheetAmount",
    "PurchaseObligation",
    "LongTermPurchaseCommitmentAmount",
]
TAG_REVENUE = ["RevenueFromContractWithCustomerExcludingAssessedTax", "Revenues"]
```

폴백 순서대로 시도하고, 채택한 태그를 `payload.tag_used`에 기록한다. **회사마다 다른 태그를 썼다는 사실 자체가 비교가능성 정보**이므로 리포트에 노출한다.

**커스텀 태그 한계**: `companyfacts`/`companyconcept`는 us-gaap·ifrs·dei만 담고 회사 커스텀 태그는 담지 않는다. 8개 태그가 전부 404면 `available=False`, `reason='custom_tag'`로 두고 **파싱을 시도하지 말 것**.

### 5.2 EDGAR 전문검색 (`collectors/sec_fts.py`)

```
GET https://efts.sec.gov/LATEST/search-index
    ?q="financial guarantee"&forms=8-K&startdt={from}&enddt={today}&from=0
```

`[확인필요]` 비공식 엔드포인트다. 파라미터명과 응답 스키마를 실제 응답 1건으로 확정한 뒤 구현한다. 2001-05-04 이후만 색인된다. 날짜는 하드코딩하지 말고 실행 시점으로 채운다.

**용도는 후보 추출까지다(R9).** 결과는 `event_candidate`에 `review_status='pending'`으로 저장하고 멈춘다. 확정은 사람이 CLI로 한다. 검색어는 `data/queries.yaml`로 분리한다.

### 5.3 관세청 무역통계 (`collectors/kcs_trade.py`)

공공데이터포털 "관세청_품목별 수출입실적". 서비스키 필요(`.env`의 `KCS_SERVICE_KEY`).

`[확인필요]` 오퍼레이션명·파라미터·응답 형식(XML 여부)을 신청 후 실제 응답으로 확정한다.

- HS 10단위 코드는 `data/hs_codes.yaml`로 분리. 메모리 관련 코드는 사람이 지정
- **단가는 두 가지를 모두 산출한다**: `금액/중량`(kg당)과 `금액/수량`(개당). 둘의 괴리가 제품 믹스 변화 신호다
- 갱신 주기: 월 1회. 매월 15일경 정정·취하 반영이 있으므로 **과거 3개월치를 매번 재수집**하고, 값이 바뀌면 새 리비전을 쌓는다
- **재생 불가 소스다**(§9.2). 정정 이력의 vintage를 제공하지 않으므로 과거 시점에 무엇을 알 수 있었는지 복원할 수 없다

### 5.4 TWSE (`collectors/twse.py`)

```
GET https://openapi.twse.com.tw/v1/opendata/t187ap05_L
```

키 불필요. 전체 상장사 월매출이 한 번에 온다. 대상 종목만 필터. 매월 10일 마감이므로 **11일 이후에 수집**.

`[확인필요]` **이 엔드포인트가 과거 월을 주는지 당월 스냅샷만 주는지 확인할 것.** 스냅샷만이라면 §6.3의 최소 표본 24개는 실시간 축적으로만 채워지며 2년 이상 걸린다. 그 경우 MOPS 월매출 공고 등 대체 이력 소스를 조사해 사람에게 보고하고, 확보 전까지 해당 지표는 비활성으로 둔다. **이력이 없다는 이유로 최소 표본을 낮추지 말 것.**

### 5.5 FRED (`collectors/fred.py`)

```
GET https://api.stlouisfed.org/fred/series/observations
    ?series_id=DFII10&api_key={key}&file_type=json
```

배경 띠 전용. **어떤 전제에도 연결하지 않는다.** 시리즈: `DFII10`(10년 실질금리), `BAMLC0A0CM`(IG OAS), `BAMLH0A0HYM2`(HY OAS).

---

## 6. 지표 명세

### 6.0 공통 인터페이스

```python
@dataclass
class IndicatorResult:
    indicator_id: str
    period: str
    value: float | None
    unit: str
    available: bool
    reason: str | None     # available=False면 필수
    payload: dict          # 리포트·감사용. 규칙은 읽을 수 없다 (R11)
    known_at: str
    source_url: str | None

class Indicator(Protocol):
    id: str
    frequency: Literal["daily","monthly","quarterly","event"]
    replayable: bool       # False면 백필에서 제외 (§9.2)
    def compute(self, db, as_of: date) -> list[IndicatorResult]: ...
```

- 지표는 `observation_current` 뷰만 읽는다
- `as_of`가 주어지면 `known_at <= as_of`인 관측만 사용한다(R12). 이 필터는 지표가 아니라 `clock.py`가 강제하며, 우회 조회는 예외를 던진다
- **파생지표 화이트리스트**: 다른 지표의 결과를 입력받는 지표는 `indicators/__init__.py`의 `DERIVED = {...}`에 등록된 것만 허용. 미등록 지표가 다른 지표를 참조하면 기동 실패(R1). 파생지표는 규칙에 연결할 수 없다

### 6.1 자립도 — `self_funding_hyper` / `self_funding_neo` / `self_funding_gap` (분기)

```
funding_gap_usd(cohort) = Σ capex − Σ ocf          # 외부조달 필요액
self_funding_hyper      = funding_gap_usd(4사)   / Σ revenue(4사)
self_funding_neo        = funding_gap_usd(네오)  / Σ revenue(네오)
self_funding_gap        = neo − hyper              # 파생지표. 규칙 연결 금지
```

비율(capex/ocf)이 아니라 매출로 정규화한 갭을 쓴다. 네오클라우드는 OCF가 음수인 경우가 흔한데, 비율 지표는 분모가 0에 수렴하면 발산하고 음수 분모는 부호가 뒤집힌다. 합산 갭은 음수 OCF를 그대로 흡수하고, 매출 정규화는 스케일이 다른 두 코호트를 같은 축에 놓는다. **정규화가 부호를 바꾸지 않는다는 점이 중요하다** — §8의 기준점 0이 정의 변경에 영향받지 않는 이유다.

- 코호트: `data/cohorts.yaml`, `as_of` 기준 구성(§4.4). 구성이 바뀌면 **과거 값을 소급 수정하지 않고** 새 구성으로 이어 붙이며 리포트에 구성 변경 표시
- **제외 기준**: `capex`, `ocf`, `revenue` 중 하나라도 결측인 기업만 제외한다. **OCF가 음수라는 이유로 제외하지 않는다**
- `payload.excluded_missing`(결측 제외)과 `payload.excluded_policy`(정책 제외, 현재 비어 있음)를 분리 기록
- 코호트 중 결측 기업이 절반을 넘으면 `available=False`, `reason='cohort_coverage<50%'`
- `self_funding_gap`은 hyper·neo가 모두 available일 때만 산출
- `replayable = True`

### 6.2 `offbalance_notional` — 부외 익스포저 (분기)

**초기 상태: 비활성.** `available=False`, `reason`은 선행 명세의 사유를 그대로 사용.

재정의안은 배수 폐기 후 둘로 분리:
- `offbalance_scale` = 명목보증 / 매출 — 익스포저 크기
- `offbalance_optimism` = 공정가치 / 명목보증 — 회사의 낙관 정도

이유: 명목÷공정가치는 분모가 회사의 불이행 확률 모델 산출물이라, 회사가 낙관적일수록 분모가 0에 수렴하며 배수가 발산한다. 위험의 크기와 낙관의 크기가 한 숫자에 섞인다.

### 6.3 `supply_chain_transmission` — 공급망 가격·매출 전이 (월간)

```
value = corr( Δlog(메모리 단가), Δlog(대만 AI체인 월매출) )  롤링
```

- **최소 표본은 차분 후 24개(원계열 25개월)**. 미만이면 `available=False`, `reason='insufficient_sample:24'`. 부분 표본 계산 금지
- 표본이 찰 때까지 **수집만 하고 판정 규칙은 등록하지 않는다**
- 점추정과 함께 부트스트랩 95% 신뢰구간을 `payload.ci`에 넣는다. **신뢰구간이 0을 포함하면 규칙은 `undetermined`를 낸다**
- `payload`에 kg당·개당 단가 두 계열과 괴리율 저장
- 이름 주의: 비용 전가율이 아니다. 수요 실재성의 간접 프록시에 가깝다
- `replayable = False` (§9.2)

### 6.4 `commitment_concentration` — 약정 집중도 (이벤트)

```
HHI = Σ (counterparty_share)^2,  dup_status='duplicate' 및 amount_usd IS NULL 제외 후
```

- **HHI는 취약성 판정이 아니라 추가검증 트리거다.** 어떤 전제도 직접 `true`/`false`로 만들지 않는다. 임계 초과 시 `event_candidate`에 검토 항목을 생성하는 것까지가 전부
- `unchecked` 금액 비중이 임계 초과면 `available=False`, `reason='dup_unchecked'`
- `amount_usd IS NULL` 금액 비중이 임계 초과면 `available=False`, `reason='amount_missing'`
- 임계 이하이면 계산하되 `payload.unchecked_share`, `payload.null_share`를 채우고 리포트에 표시
- `replayable = False` — 사람 검토 시각(`reviewed_at`)이 전부 현재이므로 과거 시점 재구성 불가

### 6.5 `kr_transmission` — 한국 전이 (월간)

수집만 하고 판정 규칙은 **등록하지 않는다.** 동조 여부를 말하려면 최소 8분기 표본이 필요하다. `replayable = False`.

---

## 7. 판정 엔진 (`rules/engine.py`)

```python
@dataclass
class Verdict:
    premise_id: str
    status: Literal["true","false","undetermined"]   # undecidable은 사람만 지정
    reason: str
    observation_id: int | None

@dataclass
class RuleContext:
    obs: IndicatorResult          # 단일 지표. payload 제거 상태로 주입 (R11)
    thresholds: RuleThresholds    # anchor, buffer, hysteresis
    premises: dict[str, Premise]  # 읽기 전용 스냅샷
    as_of: date
    history: list[float]          # min_dwell 판정용, as_of 이전 값만 (R12)
    # db 핸들은 주지 않는다 — 규칙은 부작용을 가질 수 없다

Rule = Callable[[RuleContext], list[Verdict]]
```

**실행 순서** — 하나의 `as_of` 틱에 대해:

1. 지표 계산 → `observation`에 저장. 기존 값과 다르면 새 리비전, 같으면 아무것도 하지 않음
2. 만료 검사(R10): 근거 지표가 연속 N기간 미관측이면 `undetermined` 복귀 `Verdict` 생성. `actor='rule:staleness'`, `reason='evidence_stale:<n>기간 미관측'`
3. `available=False`인 지표에 묶인 판정 규칙은 **호출하지 않는다**(R7)
4. `enabled: true`인 규칙만 실행 → `Verdict` 수집
5. 이력현상 적용(§8.3): 진입·이탈선과 최소 체류 조건을 만족하지 않는 `Verdict`는 폐기
6. 가드:
   - 대상 전제의 현재 status가 `undecidable`이면 폐기(R3) — 만료 규칙에도 동일 적용
   - `Verdict.premise_id`가 규칙의 `premises` 선언에 없으면 예외
   - `reason`이 빈 문자열이면 예외
7. 현재 status와 동일하면 폐기(R8)
8. 남은 것만 `ledger` insert + `premise.status` 갱신. `evidence.observed`가 없으면 insert 거부

**규칙 등록** — 대상 전제를 코드에 하드코딩하지 않고 선언한다.

```python
@rule(id="self_funding_threshold",
      indicator="self_funding_hyper",
      premises=["P-A-03", "P-B-07"])
def self_funding_threshold(ctx: RuleContext) -> list[Verdict]: ...
```

기동 시 검증: `premises`의 모든 ID가 `premises.yaml`에 존재하고, 각 전제의 `indicator_id`가 `indicator`와 일치할 것. 불일치는 기동 실패.

---

## 8. 판정선 결정 절차

**이 절은 에이전트가 숫자를 채우는 곳이 아니다.** 절차를 실행해 산출물을 사람에게 제출하는 것까지가 작업이다.

판정선은 두 개의 독립된 질문으로 나뉜다. 섞으면 백필 결과에 맞춰 선을 긋는 인샘플 피팅이 된다.

### 8.1 기준점(anchor) — 전제 문장에서 유도한다

**질문: 주장이 참이 되는 경계는 어디인가.**

이 답은 데이터를 보기 전에 정해진다. P-A-03 "하이퍼스케일러는 영업현금흐름만으로 CAPEX를 감당하지 못한다"의 경계는 `Σcapex > Σocf`, 즉 **gap > 0**이다. 0은 분포에서 뽑은 게 아니라 문장의 의미에서 나온다.

**R13**: 모든 규칙은 `anchor_method='semantic'`과 `anchor_rationale`을 가져야 활성화된다. 백필 분포를 보고 정한 anchor는 허용하지 않는다.

**부수 산출물 — 전제 짝 검토.** anchor를 유도하다 보면 버블측·반박측이 서로의 부정이 아닌 짝이 드러난다. P-A-03의 짝이 "감당 가능하다"면 0에서 깔끔하게 갈리지만, "일시적 초과일 뿐 구조적이지 않다"라면 gap의 부호로는 판정할 수 없다. 그런 짝은 다음 중 하나로 처리한다.

- 지속 기간을 보는 별도 지표를 정의한다
- 해당 전제를 `undecidable`로 분류한다
- 지표 연결을 끊고 `indicator_id: null`로 둔다

**하나의 규칙이 두 전제를 반대로 뒤집는 설계는 이 검토를 통과한 짝에만 쓴다.**

### 8.2 완충폭(buffer) — 잡음 규모에서 산출한다

**질문: 얼마나 움직여야 경계를 넘었다고 말할 것인가.**

여기서만 데이터를 본다. 단 **판정 결과가 아니라 계열의 물리적 성질만** 본다.

```
buffer = median( |v(q) − v(q−1)| )   # 백필 구간의 기간간 변동 중앙값
```

"이 지표가 아무 일 없을 때 통상 얼마나 움직이는가"이다. 판정을 그럴듯하게 만들려는 의도가 들어갈 자리가 없다. 표준편차 대신 중앙값을 쓰는 이유는 코호트 구성 변경 같은 한 번짜리 점프에 끌려가지 않기 위해서다.

`undetermined` 구간은 `anchor ± buffer`가 된다.

### 8.3 이력현상 — 진동은 완충폭으로 잡지 않는다

값이 경계 근처에 머물면 상태가 반복해서 뒤집힌다. 이걸 buffer를 넓혀서 막으면 다시 결과에 맞추는 것이다. 별도 장치로 처리한다.

- **비대칭 진입·이탈**: `v > anchor + buffer`에서 진입, 이탈은 `v < anchor − buffer`에서만
- **최소 체류**: 전이는 연속 `min_dwell_periods` 기간 조건을 만족해야 발생. 한 기간짜리 스파이크로는 원장이 움직이지 않는다

**조정이 필요하면 조정 대상은 `min_dwell_periods`이지 anchor가 아니다.**

### 8.4 `thresholds.yaml`

숫자만 남으면 반년 뒤에 왜 그 숫자인지 아무도 모른다. 근거를 같이 적는다.

```yaml
rules:
  self_funding_threshold:
    enabled: false                   # §8.5 절차 완료 후 사람이 true로
    anchor: 0.0
    anchor_method: semantic          # R13. 'semantic' 외의 값은 거부
    anchor_premise: P-A-03
    anchor_rationale: "Σcapex > Σocf 이면 자체 현금흐름으로 감당 불가"
    buffer: null                     # §8.2로 산출 후 채움
    buffer_method: median_abs_diff
    buffer_window: null              # [from, to]
    buffer_derived_at: null
    hysteresis:
      enter_at: +buffer
      exit_at: -buffer
      min_dwell_periods: 2

staleness:                           # R10
  quarterly: 2
  monthly: 3
  daily: 10

supply_chain:
  min_samples: 24

commitment:
  unchecked_share_max: 0.05
  null_amount_share_max: 0.10

nowcast:
  min_samples_for_band: 4
```

`anchor`는 데이터가 늘어도 재산출하지 않는다. `buffer`는 재산출할 수 있으나 그때마다 `human:threshold_change`로 원장에 남기고 **소급 재판정은 하지 않는다.**

### 8.5 확정 절차

1. 전제별로 anchor를 유도하고 `anchor_rationale`을 채운다 (사람)
2. 유도 불가한 전제를 §8.1의 세 갈래로 분류한다 (사람)
3. `bm backfill --dry-run`으로 지표 계열을 산출한다 (에이전트)
4. `bm thresholds derive`로 buffer를 계산한다 (에이전트, 기계적)
5. dry-run 리포트를 검토한다 (사람) — §8.6
6. `enabled: true`로 바꾸고 `bm backfill --commit`을 실행한다 (사람 승인 후)

### 8.6 백필 dry-run이 먼저 답해야 할 것

**지표가 판정할 게 있는지부터 본다.** dry-run 리포트에 다음을 출력한다.

| 항목 | 무엇을 보는가 |
|---|---|
| 지표별 anchor 기준 부호 변화 횟수 | 0이면 전제가 자명하게 참/거짓이고 지표에 정보가 없다 |
| 전제별 상태 전이 횟수 | 백필 기간 수를 넘으면 잡음을 잡고 있다 |
| `undetermined` 구간 체류 비율 | 대부분이면 buffer가 과하거나 지표가 무력하다 |
| 지표별 결측·비활성 기간 수 | 재생 구간에 구멍이 얼마나 있는지 |

**부호 변화가 0인 경우는 실패가 아니라 발견이다.** 그 전제는 양측이 동의하는 사실이므로 `side: shared`로 옮기고, 진짜 쟁점이 무엇인지 다시 묻는다. 자립도의 경우 아마 "격차가 벌어지고 있는가"일 텐데, 그러면 `self_funding_gap`을 파생지표에서 빼내 규칙에 연결하는 문제가 된다. 현재 설계는 그것을 금지하고 있으므로 §6.0의 화이트리스트와 R1을 함께 재검토해야 한다. **에이전트가 임의로 바꾸지 말고 사람에게 보고한다.**

---

## 9. 백필과 시간순 재생 (`backfill.py`)

### 9.1 재생 시계

**틱 단위는 기간이 아니라 공시일이다.** 2025Q2 값은 7월 말 10-Q가 나와야 공개된다. `as_of`를 기간 종료일로 잡으면 한 달 앞선 정보를 쓰게 된다.

```
1. window_from ~ window_to 사이의 모든 대상 기업 제출일을 모아 정렬한다
   (submissions API의 10-Q/10-K filingDate)
2. 각 제출일을 as_of로 삼아 오름차순으로 틱을 돌린다
3. 각 틱에서: 지표 계산(known_at <= as_of) -> 판정 -> 원장 기록
4. 원장 행은 as_of=틱 날짜, recorded_at=현재 시각,
   origin='replay', run_id=<이번 런>
```

한 틱에 코호트 중 일부만 제출한 상태가 정상이다. 4사 중 하나만 냈으면 커버리지 25%로 `available=False`가 되고 규칙은 건너뛴다. 라이브에서 실제로 일어나는 일이 재생에서도 그대로 재현된다.

**라이브도 같은 축을 쓴다.** `as_of`는 탐지 시각이 아니라 공시일이다. 그래야 재생 구간과 라이브 구간이 하나의 시간축에 놓인다. `recorded_at`만 실제 처리 시각이다.

### 9.2 재생 가능 소스

| 소스 | 재생 | 이유 |
|---|---|---|
| SEC XBRL | 가능 | 사실마다 `filed` 존재 |
| SEC FTS / commitment | **불가** | 문서는 `filed_at`이 있으나 사람 검토(`reviewed_at`)가 전부 현재. 과거를 재생하면 나중에 확정한 약정이 보인다 |
| 관세청 | **불가** | 정정 이력의 vintage 미제공. 지금 받는 값은 정정 후 값 |
| TWSE | **불가** | 당월 스냅샷 (`[확인필요]` §5.4) |
| FRED | 가능하나 무관 | 규칙 미연결 |

**백필은 `replayable=True`인 지표만 대상으로 한다.** 나머지는 라이브로만 시작한다. 이 제약을 어기면 없던 역사가 만들어진다. `bm backfill`은 `replayable=False` 지표를 요청받으면 거부한다.

### 9.3 룩어헤드 가드 (`clock.py`)

R12를 사람의 주의력에 맡기지 않는다.

```python
class ReplayClock:
    """as_of가 설정되면 이후 정보 접근을 차단한다."""
    def facts(self, ...) -> list[dict]:   # filed <= as_of 자동 필터
    def cohort(self, name) -> list[str]:  # as_of 기준 구성 (§4.4)
    def observations(self, ...):          # known_at <= as_of 자동 필터
```

- 재생 모드에서 `ReplayClock`을 거치지 않은 DB·수집기 접근은 **예외를 던진다**
- 테스트: 미래 `filed`가 섞인 픽스처로 재생했을 때 결과가 달라지지 않을 것

### 9.4 런 관리와 재실행

개발 중 지표 버그는 확실히 난다. R5 때문에 잘못된 재생 결과를 지울 수 없다. 소프트 삭제로 흐르지 않으면서 재실행을 허용하는 방법:

- 원장 행은 **하나도 지우지 않는다**
- 잘못된 런은 `ledger_run.status='superseded'`로 표시하고 `superseded_by`, `supersede_reason`을 채운다
- 리포트·API는 `ledger_effective` 뷰(active 런 + 라이브)만 읽는다
- 재실행은 새 `run_id`와 함께 `--supersede <old_run_id> --reason TEXT`를 요구한다
- `code_version`(git sha)과 `thresholds_hash`를 런에 기록한다 — 어떤 규칙 버전이 어떤 역사를 만들었는지가 남는다

**폐기된 재생도 원장에 남는다.** 이것이 소프트 삭제와 다른 점이다.

### 9.5 dry-run이 기본

```
bm backfill --from 2024-01-01 [--to DATE] [--indicator I]
                              [--commit] [--supersede RUN --reason TEXT]
```

`--commit` 없이는 원장에 쓰지 않고 §8.6의 리포트만 출력한다. 되돌릴 수 없는 행위이므로 기본값을 안전한 쪽에 둔다.

### 9.6 재생하지 않는 것

- **나우캐스트**: 예측은 공시 전에만 의미가 있으므로 소급 생성이 불가능하다. 백필로 채우려 들지 말 것. 밴드는 라이브 가동 후 표본이 쌓여야 켜지며 최소 1년이 걸린다
- **사람 판정**: `actor='human:*'` 행은 재생이 만들지 않는다
- **판정선 변경 이력**: 소급 재판정 금지(§8.4)

---

## 10. 나우캐스트 오차 (`nowcast.py`)

프록시 신뢰도의 유일한 근거다. **v0부터 켠다.** 다만 §9.6대로 재생 불가다.

**예측값은 `nowcast_error`에만 저장한다. `observation`에는 어떤 경우에도 들어가지 않는다.** R6은 관측 저장소에 대한 규칙이며 예측 저장소에는 적용되지 않는다.

1. 분기 공시 전, 월간 프록시로 해당 분기 값을 예측해 `predicted`, `predicted_at`, `days_ahead` 저장
2. 실제 공시가 들어오면 같은 `(indicator_id, target_period)`의 **모든 예측 행**에 `actual`, `actual_at`, `abs_error`를 채운다. days_ahead별 오차를 남기는 것이 목적이므로 하나의 actual이 여러 행에 들어가는 것이 정상이다
3. 리포트에 지표별 최근 4회 평균 절대오차 표시
4. 표본이 `thresholds.nowcast.min_samples_for_band` 미만이면 오차를 표시하지 않고 "표본 부족"이라고 쓴다. API는 `band: null` 반환

밴드 폭: `band(t) = mae * (1 + elapsed_days / period_days)`. 공시일에 리셋.

---

## 11. 스케줄러

cron 또는 GitHub Actions.

| 작업 | 주기 | 명령 |
|---|---|---|
| 배경 띠 | 평일 06:00 KST | `bm collect --group daily` |
| 이벤트 후보 | 매일 07:00 KST | `bm collect --group events` |
| 대만 월매출 | 매월 11일 | `bm collect --group monthly-tw` |
| 관세청 | 매월 16일 | `bm collect --group monthly-kr` |
| 분기 공시 | 매일 (신규 제출 감지) | `bm collect --group quarterly` |
| 판정+리포트 | 위 작업 직후 | `bm evaluate && bm report` |

그룹을 분리한 이유: 하나로 묶으면 11일에 관세청을 정정 반영 전에 긁고, 16일에 TWSE를 불필요하게 재수집한다.

분기 수집은 날짜 기반이 아니라 `submissions` API의 신규 10-Q/10-K 감지로 트리거한다. 백필은 스케줄에 넣지 않는다 — 수동 실행 전용이다.

---

## 12. CLI (`cli.py`)

```
bm init                       DB 생성, 마이그레이션, premises.yaml 시드
bm collect [--group G] [--indicator I] [--offline] [--no-cache]
bm evaluate [--dry-run]       판정. --dry-run은 원장에 쓰지 않고 변경 예정만 출력
bm backfill --from DATE [--to DATE] [--indicator I]
            [--commit] [--supersede RUN --reason TEXT]
                              시간순 재생. --commit 없으면 dry-run (§9.5)
bm thresholds derive [--indicator I]
                              buffer 산출. anchor는 건드리지 않는다 (§8.2)
bm thresholds show            현재 판정선과 근거 출력
bm runs                       ledger_run 목록과 상태
bm report [--format text|json]
bm log [--premise ID] [--since DATE] [--origin live|replay]
bm review                     event_candidate 대화형 검토 (confirm/reject)
bm premise set-status ID STATUS --reason TEXT --doc DOC_ID
                              사람의 수동 판정. actor='human:<user>'로 기록
bm nowcast                    오차 현황
bm doctor                     설정·키 점검 + 코호트 전 종목의 태그 가용성 표
```

`bm evaluate --dry-run`은 CI에서 항상 통과해야 한다.

---

## 13. 읽기 전용 API (`api.py`)

FastAPI. `archived_at`이 찍힌 전제는 모든 응답에서 제외한다. 원장은 `ledger_effective` 뷰만 읽는다.

```
GET /v1/cruxes
  -> [{crux, kind: "interpretive"|"unobserved"|"observed_pending"|"verified",
       premises: [{id, side, claim, status, indicator_id, last_change_as_of}]}]

GET /v1/indicators
  -> [{id, frequency, replayable,
       latest: {period, revision, value, unit, available, reason, known_at},
       band: {mae, width, samples} | null, last_disclosure}]

GET /v1/ledger?since=&premise_id=&origin=&limit=
  -> [{as_of, recorded_at, origin, run_id, premise_id, crux, claim,
       status_from, status_to, reason, actor, evidence[]}]

GET /v1/runs
  -> [{run_id, window_from, window_to, status, code_version,
       thresholds_hash, superseded_by}]

GET /v1/unobserved
  -> [{premise_id, claim, why_unobservable}]

GET /v1/shared
  -> [{premise_id, claim, status}]
```

**`kind` 판정** — 위에서부터 먼저 맞는 것을 채택한다.

```
1) 전제 중 하나라도 undecidable                     -> "interpretive"
2) 모든 전제가 indicator_id null 또는 지표 비활성    -> "unobserved"
3) 모든 전제가 undetermined                         -> "observed_pending"
4) 그 외 (최소 1개가 true/false)                    -> "verified"
```

3번을 분리한 이유: 지표는 붙었으나 아직 아무것도 확정되지 않은 crux를 `verified`로 보내면 라벨이 실제를 오도한다.

`origin`을 응답에 반드시 포함한다. **화면은 재생 구간과 라이브 구간을 시각적으로 구분해야 한다.** 구분 없이 한 줄로 그리면 시스템이 예언자처럼 보인다.

**종합 점수 필드를 추가하지 않는다(R1).** 프런트가 요청해도 거부한다.

---

## 14. 작업 목록

의존관계 순. 각 작업은 수용 기준을 통과해야 완료다. **작업 매핑의 진실은 이 표 하나다.**

| ID | 작업 | 의존 | 수용 기준 |
|---|---|---|---|
| T1 | 스캐폴딩, `pyproject.toml`, `.env.example`, 로깅 | — | `bm --help` 동작 |
| T2 | `db.py` + `001_init.sql` + 마이그레이션 러너 | T1 | ledger UPDATE/DELETE, observation DELETE, observation의 superseded_at 외 컬럼 UPDATE가 모두 예외. `observation_current`·`ledger_effective` 뷰 동작 |
| T3 | `http.py` 레이트리밋·재시도·캐시·`--offline` | T1 | SEC 8 req/s 초과 없음. offline 모드에서 소켓 호출 0회 |
| T4 | `sec_xbrl.py` — 티커 해석, 개념 조회, YTD 차분, direct/diff 우선순위, 달력분기 정렬, **`filed` 보존** | T3 | 엔비디아·브로드컴 8분기가 달력 분기에 올바로 배정. `payload.derivation`·`tag_used`·`known_at` 기록 |
| T5 | `doctor.py` + `bm doctor` | T4 | 코호트 전 종목의 자립도 4태그 + 부외 8태그 가용성 표 출력 |
| T6 | `clock.py` — 재생 시계와 룩어헤드 가드 | T4, T2 | 미래 `filed`가 섞인 픽스처로 재생해도 결과 불변. ReplayClock 우회 접근이 예외를 던짐 |
| T7 | `indicators/self_funding.py` 3종 + `models.py` | T6 | 8분기 백필 성공. OCF 음수 기업이 제외되지 않고 합산됨. `excluded_missing`/`excluded_policy` 분리. `self_funding_gap`이 규칙에 연결 불가 |
| T8 | `thresholds.py` — 로딩·검증·`bm thresholds derive/show` | T7 | `anchor_method != 'semantic'`이거나 `anchor_rationale`이 비면 활성화 거부(R13). buffer 산출이 median_abs_diff와 일치 |
| T9 | `rules/engine.py` + 가드 + 만료 + 이력현상 + `definitions.py` | T8 | R3·R7·R8·R10·R11 회귀 테스트 통과. min_dwell 미달 전이가 폐기됨. 규칙이 payload·db 접근 불가 |
| T10 | `ledger.py` + `ledger_run` + `bm log` + `bm runs` | T9 | 두 번 실행 시 두 번째 0건. `observed` 없는 규칙 행 insert 거부. superseded 런이 `ledger_effective`에서 빠짐 |
| T11 | `backfill.py` + `bm backfill` (dry-run) | T10 | `replayable=False` 지표 요청 시 거부. §8.6의 4개 항목이 리포트에 출력. `--commit` 없이는 원장 0건 |
| T12 | **판정선 확정** (사람 게이트) | T11 | §8.5의 6단계 완료. anchor 유도 문서화, 유도 불가 전제 분류, buffer 산출·기록, dry-run 검토 결과 문서화 |
| T13 | `report.py` + `bm report` | T10 | 미관측·비활성·정정(신규 리비전)·재생 구간이 명시적으로 출력 |
| T14 | `nowcast.py` | T7 | 표본 4회 미만이면 오차 미표시. 예측값이 `observation`에 없음. 백필이 nowcast를 건드리지 않음 |
| T15 | `api.py` | T13, T14 | 종합 점수 필드 없음. `band`가 samples<4에서 null. `kind` 4종 분기. `origin` 노출 |
| T16 | `kcs_trade.py` + `twse.py` + `supply_chain.py` | T3 | 단가 2종 산출. 차분 후 표본 24 미만이면 비활성. `replayable=False`. TWSE 이력 가용성 조사 결과 문서화 |
| T17 | `sec_fts.py` + `event_candidate` + `bm review` | T3, T2 | 자동 확정 경로가 코드에 없음(R9). 공백만 다른 excerpt가 중복 차단 |
| T18 | `commitment` 원장 + 중복 플래그 + `commitment_concentration` | T17 | HHI가 어떤 전제도 직접 변경하지 않음. unchecked 임계 초과 시 비활성 |
| T19 | 부외 지표 재정의 (`offbalance_scale` / `offbalance_optimism`) | T5 | `bm doctor` 결과를 근거로 채택/폐기 문서화 |
| T20 | `fred.py` 배경 띠 | T3 | 규칙 레지스트리에 fred 지표가 하나도 없음 |
| T21 | `/` 공개 화면 골격 — 쟁점 현황판 + 원장 타임라인 + 빈 상태 | T15 | 판정선 확정 전에도 화면이 오류로 보이지 않음(§21). 재생·라이브가 배경과 라벨로 구분되고 선 스타일은 동일 |
| T22 | 증거 패널 — 원장 행에서 근거 스냅샷 열람 | T21 | 모든 규칙 행에서 `evidence.observed`와 `known_at`이 한 번의 조작으로 열림. 모바일에서 전체 화면 시트 |
| T23 | 지표 탐색기 화면 | T21, T14 | 결측을 선으로 잇지 않음(U2). `band=null`이면 밴드 미표시. 완충구간 음영이 `buffer` 확정 후에만 그려짐 |
| T24 | `/ops` 운영 화면 — 후보 검토 큐 + 재생 런 목록 | T21, T17, T11 | 인증 없이는 접근 불가. `suggested_kind`별로 액션이 갈림. superseded 런이 목록에 남음 |

**T1~T13이 백엔드 MVP, T21과 T22가 화면 MVP다.** T12(판정선 확정)를 통과하기 전에는 어떤 규칙도 `enabled: true`가 될 수 없고, 따라서 원장에 규칙 기반 행이 한 건도 쌓이지 않는다. 이것이 정상 상태다.

---

## 15. 테스트 요구사항

- **픽스처는 실제 응답을 녹화**해 `tests/fixtures/`에 둔다. 손으로 만든 JSON 금지 — 스키마 착각을 잡지 못한다
- 네트워크를 타는 테스트는 `@pytest.mark.network`로 분리하고 CI 기본 실행에서 제외
- 불변 규칙 **R3·R5·R7·R8·R9·R10·R11·R12·R13**은 각각 전용 회귀 테스트를 둔다. 수정하려면 사람 승인이 필요하다는 주석을 단다
- 골든 테스트: 고정 픽스처로 `bm collect --offline && bm evaluate && bm report` 출력 스냅샷 비교
- **정정 시나리오**: 같은 period에 다른 값을 두 번 수집했을 때 (a) 리비전 2 생성 (b) 리비전 1 잔존 (c) 기존 원장 행의 `evidence.observed.value` 불변
- **룩어헤드 시나리오**: 픽스처에 미래 `filed` 사실을 섞고 재생했을 때 결과가 섞지 않았을 때와 동일할 것
- **런 관리 시나리오**: 재생 → supersede → 재실행 후 `ledger_effective`에 두 번째 런만 보이고 `ledger` 원본에는 둘 다 남을 것

---

## 16. 설정 (`.env.example`)

```
SEC_USER_AGENT="bubble-monitor/0.1 you@example.com"
FRED_API_KEY=
KCS_SERVICE_KEY=
DB_PATH=./data/bm.sqlite3
CACHE_DIR=./.cache
LOG_LEVEL=INFO
```

`SEC_USER_AGENT`가 비어 있으면 기동 시 즉시 실패한다. 403을 런타임에 만나는 것보다 낫다.

---

## 17. 실패 모드 카탈로그

에이전트는 아래를 예외가 아니라 **정상 경로**로 처리한다.

| 상황 | 처리 |
|---|---|
| 태그 404 | 폴백 태그 시도 후에도 없으면 `available=False, reason='tag_missing'` |
| 8개 태그 전부 404 | `reason='custom_tag'`. 파싱 시도 금지 |
| YTD 차분 음수 | 그 분기 폐기, `reason='ytd_diff_negative'` |
| direct와 diff 불일치 | direct 채택 + `payload.qdiff_mismatch` + 리포트 표시 |
| 코호트 절반 이상 결측 | 지표 `available=False, reason='cohort_coverage<50%'` |
| 재생 틱에 일부 기업만 제출 | 정상. 커버리지 미달이면 그 틱은 판정 없음 |
| OCF 음수 | **제외하지 않고 합산한다.** 갭 정의가 흡수한다 |
| capex/ocf/revenue 결측 | 해당 기업 제외, `payload.excluded_missing`에 기록 |
| 표본 부족 | `available=False, reason='insufficient_sample:<n>'`. 부분 계산 금지 |
| 관세청 값 소급 정정 | 새 리비전 insert + 이전 리비전 `superseded_at` + 리포트 표시. 기존 원장 불변 |
| 정정이 판정선을 넘음 | 소급 재판정 금지. `event_candidate`에 `restatement_review` 생성, 사람이 처리 |
| 근거 지표 연속 N기간 미관측 | 전제를 `undetermined`로 복귀 + `rule:staleness` 기록 (R10) |
| 재생 요청이 `replayable=False` 지표 포함 | 거부하고 사유 출력. 부분 재생으로 진행하지 말 것 |
| 재생 결과가 잘못됨 | 런을 `superseded` 처리 후 새 런. 원장 행은 지우지 않는다 |
| dry-run에서 부호 변화 0 | 실패 아님. 전제를 `shared`로 재분류할지 사람에게 보고 (§8.6) |
| dry-run에서 전이 과다 | `min_dwell_periods` 조정. **anchor는 건드리지 않는다** |
| EDGAR FTS 스키마 변경 | 수집 실패로 처리하고 후보 0건. 기존 원장 불변 |
| 판정선 변경 | 원장에 `human:threshold_change`로 기록. 소급 재판정 금지 |
| TWSE 이력 미제공 | 대체 소스 조사 후 사람에게 보고. **최소 표본을 낮추지 말 것** |

---

## 18. 완료의 정의

**MVP(T1~T13)** 완료 조건:

1. `bm collect && bm evaluate && bm report`가 실제 SEC 데이터로 동작한다
2. 자립도 8분기가 백필돼 있고, 회계연도가 다른 기업들이 같은 달력 분기에 정렬돼 있다
3. 미래 `filed`가 섞인 픽스처로 재생해도 결과가 바뀌지 않는다 (R12)
4. 부외 지표가 비활성 상태로, 사유와 함께 출력된다
5. `ledger`에 UPDATE를, `observation`에 DELETE를 시도하면 실패한다
6. 관측값 정정이 리비전으로 쌓이고, 기존 원장 행의 근거 스냅샷이 바뀌지 않는다
7. 재생 런을 supersede하면 리포트에서 사라지지만 `ledger` 원본에는 남는다
8. 모든 활성 규칙이 `anchor_method='semantic'`과 `anchor_rationale`을 가진다 (R13)
9. 리포트가 재생 구간과 라이브 구간을 구분해 출력한다
10. 두 번 연속 실행 시 두 번째는 원장에 0건을 쓴다

**10번이 가장 중요하다.** 변경이 없을 때 침묵하는 것이 이 시스템의 핵심 동작이다.

**T15(API) 완료 조건**: `/v1/cruxes` 응답에 `interpretive` / `unobserved` / `observed_pending` / `verified` 네 종류가 모두 나타나고, `/v1/ledger`가 `origin`을 구분해 반환한다.

**화면 MVP(T21, T22) 완료 조건**:

1. 판정선 확정 전 화면이 오류나 미완성으로 보이지 않는다 (§21)
2. 미관측·해석차이 전제에 사유가 함께 표시된다 (U9)
3. 재생과 라이브가 배경과 라벨로 구분되며 선 스타일은 동일하다 (§19.1)
4. 모든 규칙 원장 행에서 `evidence.observed`와 `known_at`이 한 번의 조작으로 열린다 (U4)
5. 만료 행이 증거 링크 대신 설명을 표시한다 (§20.5)
6. 헤더에 시스템 상태 배지가 없다 (§20.2)

---

## 19. 화면 설계 원칙

원장이 상품이라는 주장은 화면에서 이행되거나 무너진다. 아래는 §1의 불변 규칙에 대응하는 UI 측 규칙이다. 위반은 스타일 문제가 아니라 제품이 거짓말을 하는 문제다.

| # | 규칙 | 대응 |
|---|---|---|
| U1 | 종합 점수·게이지·0~100 스케일을 그리지 않는다. 복수 지표를 한 축에 겹치지 않는다 | R1 |
| U2 | **결측 구간을 선으로 잇지 않는다.** 빈칸으로 두고 사유를 함께 쓴다 | R6 |
| U3 | 재생 구간과 라이브 구간을 시각적으로 구분한다 | §9 |
| U4 | 모든 상태 변화는 근거 인용과 원문 링크에 한 번의 조작으로 도달한다 | §2 따름정리 1 |
| U5 | 적/녹 신호등 색과 매매 어휘를 쓰지 않는다 | R2 |
| U6 | `undecidable`과 `undetermined`를 같은 시각 언어로 쓰지 않는다 | R3 |
| U7 | `band`가 null이면 밴드를 그리지 않는다 | §10 |
| U8 | 빈 원장은 오류 상태가 아니다 | §21 |
| U9 | 미관측·비활성에는 항상 사유를 함께 쓴다. 라벨만 두지 않는다 | R6, §17 |
| U10 | 판정선이 확정되기 전에는 완충구간을 그리지 않는다 | R13, §8 |

### 19.1 재생 구간에 점선을 쓰지 않는다 (U3의 구체화)

점선은 관례적으로 추정·예측을 뜻한다. 재생 구간은 추정이 아니라 그 시점에 실제로 알 수 있었던 관측값이다(R12). 점선으로 그리면 두 가지가 동시에 깨진다. 사용자가 재생 구간을 신뢰도 낮은 데이터로 읽고, 진짜 추정인 나우캐스트 밴드와 시각 언어가 충돌한다.

**구분은 배경 톤과 라벨과 divider로 한다. 선 스타일은 라이브와 동일하게 둔다.** 점선은 나우캐스트 밴드와 아직 확정되지 않은 판정선에만 쓴다.

### 19.2 색

적/녹은 쓰지 않는다. 초록은 매수로, 빨강은 손실로 읽힌다(R2). 상태는 다음 네 갈래로만 구분한다.

| 상태 | 시각 처리 |
|---|---|
| `true` | 채워진 칩. 강조색 |
| `false` | 테두리만 있는 칩. 무채색 |
| `undetermined` | 파선 테두리 칩. 흐린 글자 |
| `undecidable` | 다른 색상 계열의 칩. 위 셋과 같은 축에 놓지 않는다 (U6) |

`undecidable`을 `undetermined`의 변형으로 그리면 "아직 안 풀린 것"과 "이 지표로는 못 푸는 것"이 섞인다. 후자는 이 제품이 주장하는 바의 일부다.

### 19.3 스택

읽기 전용이고 상호작용이 적다. SPA는 과하다.

- **FastAPI + Jinja 서버 렌더.** §13의 JSON API와 같은 앱에서 HTML 라우트를 얹는다. 빌드 파이프라인이 없고 공유 링크가 그대로 열린다
- **차트는 uPlot 또는 Observable Plot.** gap 처리를 기본 지원하는 것을 고른다. 대부분의 차트 라이브러리는 null을 이어 그리는 것이 기본값이라 U2가 가장 새기 쉬운 지점이다
- 정적 생성은 원장이 계속 늘어 재빌드 주기 관리가 붙는다. React SPA는 이 정도 상호작용에 비용이 크다

---

## 20. 라우트와 화면 명세

### 20.1 라우트 분리

| 라우트 | 내용 | 인증 | 캐시 | 쓰기 |
|---|---|---|---|---|
| `/` | 쟁점 현황판, 원장 타임라인, 지표 탐색기 | 없음 | 허용 | 없음 |
| `/ops` | 위 셋 + 후보 검토 큐 + 재생 런 목록 | 필요 | 없음 | 있음 |

지금 분리하는 이유는 셋이다.

1. **쓰기 경로 때문이다.** 후보 확정·기각과 전제 상태 지정은 원장에 `human:` 행을 쓴다(R9). 읽기 전용으로 설계된 앱에 쓰기 권한을 사후에 얹으면 사고가 난다
2. **신선도 요구가 반대다.** 공개 화면은 캐시가 길어도 되고 분기 지표에는 그게 맞다. 운영 화면은 방금 들어온 후보를 봐야 한다. 섞으면 캐시 정책이 낮은 쪽으로 수렴한다
3. 지금은 라우트를 나누는 일이고 나중에는 컴포넌트를 쪼개는 일이 된다

**`/ops`는 공개 화면 컴포넌트를 재사용하되 반대는 막는다.** 운영자는 검토하면서 쟁점 카드를 봐야 하지만, 공개 화면은 후보 큐의 존재를 몰라야 한다.

### 20.2 헤더

**시스템 상태 배지를 두지 않는다.** `수집 정상`이 화면 최상단에 있으면 사용자는 그것을 시스템의 총평으로 읽고, 판정과 무관한 초록 표시가 "버블 아님"으로 오독된다.

수집 실패는 결과로 드러난다. 지표가 비활성이면 카드에 사유가 뜨고, 원장이 비면 그 자체가 신호다. 헤더 배지가 필요하다고 느껴지면 그 신호가 화면에 드러나지 않는다는 뜻이므로 배지가 아니라 그쪽을 고친다.

헤더에는 제품명과 현재 국면 라벨(`판정선 미확정` 등)만 둔다.

### 20.3 정보 흐름

```
1. 헤더            제품명 + 국면 라벨
2. 히어로          지금 시스템이 무엇을 말하고 있는지 한 문장
3. 요약 수치       추적 중인 쟁점 · 등록된 전제
4. 쟁점 현황판     쟁점 단위 그룹. 각 전제의 side와 status
5. 원장 타임라인   변경 이력. 구간 divider로 재생/라이브 분리
6. 지표 탐색기     계열 + 밴드 + 결측 + 구간 구분
7. (ops) 운영 큐   후보 검토 + 재생 런
```

**요약 수치에는 쟁점 수와 전제 수만 둔다.** 연결된 지표 수, 백필된 분기 수, 원장 기록 수는 "시스템이 무엇을 준비했나"이지 "무엇을 다투고 있나"가 아니다. 특히 원장 0을 최상단 지표로 올리면 정확히 피하려던 "고장난 대시보드" 신호가 된다. 이 수치들은 각자의 맥락이 있는 곳으로 내린다.

### 20.4 쟁점 현황판

- 전제를 평면 나열하지 않고 **쟁점 단위로 묶는다.** `/v1/cruxes`의 구조 그대로다
- 각 전제에 `side`(버블·반박·공유)를 라벨로 표시한다
- 쟁점 헤더에 `kind` 4종을 한글 라벨과 한 줄 설명으로 표시한다

| kind | 라벨 | 설명 |
|---|---|---|
| `verified` | 검증됨 | 양측 주장 중 최소 하나가 확정됨 |
| `observed_pending` | 관측대기 | 지표는 연결됐으나 아직 판정된 전제 없음 |
| `unobserved` | 미관측 | 연결된 지표가 없거나 전부 비활성 |
| `interpretive` | 해석차이 | 관측으로는 판정할 수 없는 전제가 포함됨 |

- **필터 탭은 판정이 시작된 뒤에만 노출한다.** 빈 상태에서는 두 종류가 항상 0건이라 빈 필터를 눌러보게 만든다
- **미관측·해석차이 전제에는 사유를 함께 쓴다(U9).** 내부 코드를 그대로 노출하지 않고 풀어 쓴다

| reason | 화면 문구 |
|---|---|
| `custom_tag` | 회사 자체 태그를 써서 표준 항목으로 수집되지 않습니다 |
| `cohort_coverage<50%` | 코호트 절반 이상이 결측이라 합산하지 않습니다 |
| `insufficient_sample:24` | 표본이 부족합니다 (차분 후 9 / 24) |
| `tag_missing` | 해당 항목을 공시에서 찾지 못했습니다 |
| `undecidable` 전제 | 지표의 부호로는 지속성을 가릴 수 없습니다 (전제별로 다름) |

원본 코드는 증거 패널과 `/ops`에 남긴다.

### 20.5 원장 타임라인

- 행 배지로 `origin`을 표시하고, **구간이 바뀌는 지점에 divider 행**을 넣는다 — "여기부터 재생 구간 · 2026-06-01 이전". 배지만으로는 스크롤 중 구간 전환을 인지하지 못한다
- 재생 행은 배경 톤을 한 단계 낮춘다. 선 스타일은 바꾸지 않는다(§19.1)
- **만료 행(`actor='rule:staleness'`)은 다른 문법을 쓴다.** 근거가 생겨서가 아니라 없어져서 움직인 행이므로 증거 링크가 없다. 링크 자리에 설명을 둔다 — "새 근거로 바뀐 것이 아니라 근거가 끊겨 판정이 회수된 행입니다". 사용자가 가장 헷갈리는 행이다
- 원장은 `ledger_effective` 뷰만 읽는다. superseded 런은 `/ops`에서만 보인다

### 20.6 증거 패널 (U4)

원장 행에서 한 번의 조작으로 `evidence`를 연다. 데스크톱은 우측 슬라이드 패널, **모바일은 전체 화면 시트**다.

내용: `doc_id`, `quote`, 원문 링크, 그리고 `observed` 블록 전체 — `indicator_id`, `period`, `revision`, `value`, `unit`, `known_at`, `tag_used`.

`known_at`을 노출하는 것이 중요하다. 재생 행에서 이 값이 `as_of` 이하라는 것이 R12를 지켰다는 화면상의 증거다.

### 20.7 지표 탐색기

- **결측 구간은 빗금으로 표시하고 선을 끊는다(U2).** 사유를 캡션에 쓴다 — "빗금 구간은 값이 0이 아니라 없음"
- 재생 구간은 배경 톤, 라이브 시작점에 세로 divider. 선 스타일 동일
- **밴드가 실제로 그려지는 구간은 좁다.** 재생 구간에는 밴드가 존재할 수 없고(§9.6) 라이브 초기에도 표본 4회 미만이라 `null`이다. 가동 첫 1년은 밴드가 아예 없다. 범례와 빈 상태 문구가 이를 반영해야 한다
- **완충구간 음영은 `buffer`가 확정된 뒤에만 그린다(U10).** 확정 전에는 판정선 자리를 점선으로만 표시한다
- 채택 태그(`tag_used`)와 산출 경로(`derivation`)를 노출한다. 회사마다 다른 태그를 썼다는 사실이 비교가능성 정보다

### 20.8 운영 화면 (`/ops`)

- **후보 검토 큐의 액션은 `suggested_kind`별로 갈린다.** `guarantee`·`commitment`·`investment`·`lease`는 확정/기각이지만, `restatement_review`는 확정할 대상이 아니라 사람이 전제 상태를 지정해야 하는 항목이다. 같은 버튼 쌍을 쓰면 안 된다
- **재생 런 목록**을 둔다. superseded 런이 원장에는 남지만 리포트에서 사라지는 것이 이 시스템의 특징이므로 확인할 곳이 있어야 한다. `run_id`, 창, `code_version`, `thresholds_hash`, 상태, `supersede_reason`을 표시한다

### 20.9 모바일

380px에서 2단은 무너진다. 스택 순서는 §20.3 그대로다.

- 쟁점 카드의 좌우 대립 배치는 세로 스택 + `side` 라벨로 바꾼다
- 지표 탐색기는 별도 화면으로 빼는 것을 우선 검토한다. 드롭다운과 차트가 한 화면에서 가장 손이 많이 가는 부분이다
- 증거 패널은 전체 화면 시트

---

## 21. 빈 상태 — 판정선 확정 전 화면

T12를 통과하기 전까지 규칙은 전부 `enabled: false`이고 원장에는 규칙 기반 행이 없다. **이 상태가 몇 달간의 기본 화면이다.** 예외 처리가 아니라 정식 화면으로 설계한다.

### 21.1 원칙

- **"준비 중"·"데이터를 불러오는 중"·"아직 데이터가 없습니다"를 쓰지 않는다.** 미완성 신호다
- 이 화면의 문장은 **입장 표명**이다 — "아직 아무것도 판정하지 않았습니다". 전제는 등록됐고 지표는 붙었으며, 판정선을 긋기 전까지 어느 쪽 주장도 참이라고 말하지 않는다는 뜻이다
- 진행률 표시("3단계 중 0단계 완료")는 공개 화면에 두지 않는다. 운영자에게는 유용하나 방문자에게는 미완성 신호다. `/ops`에만 둔다

### 21.2 구성

1. 헤더 — 제품명 + `판정선 미확정`
2. 히어로 — 위 문장
3. 요약 수치 — 추적 중인 쟁점, 등록된 전제
4. **판정을 시작하려면** — §8.5 절차 3단계. 순서 있는 절차이므로 번호를 붙인다. 특히 "분포를 보고 정하지 않습니다"를 노출한다. 이 제품이 무엇을 하지 않는지가 논지다
5. 쟁점별 전제 — 전부 미결. 각 전제에 사유(U9)
6. 원장 — 빈 타임라인

### 21.3 판정선 자리

쟁점 목록의 상태 칩 열에 **아직 그어지지 않은 판정선을 점선으로 표시한다.** 지금은 모든 전제가 이 선 앞에 서 있고, 선이 정해지면 칩이 양쪽으로 갈린다. 빈 상태 자체가 시스템의 동작을 설명하는 장치이며, 판정이 시작돼도 같은 축을 쓰므로 화면이 이어진다.

완충구간 음영은 여기서 그리지 않는다(U10). anchor가 유도되지 않았고 buffer가 산출 전인데 띠를 그리면 판정선이 정해진 것처럼 보인다.

### 21.4 빈 원장

**터미널 골격으로 만든다.** 고정된 "0" 카드가 아니라, 첫 기록이 그대로 쌓일 레일과 상태바를 미리 세운다. 판정이 시작돼도 레이아웃이 바뀌지 않는다.

- 상태바: `이벤트 수신 대기 중`, 채널 라벨 `실시간 · 재생`
- 본문: 빈 타임라인 레일 + 점선 눈금
- 하단: "기록된 변경이 없습니다" + "변경이 없을 때 침묵하는 것이 이 시스템의 기본 동작입니다"

마지막 문장이 이 화면의 핵심이다. 빈 원장은 결손이 아니라 §18 완료조건 10번의 정상 동작이다.

### 21.5 미해결

모바일 720px 아래에서는 판정선 점선을 숨긴다. 전제 문장과 축을 같은 폭에서 동시에 읽히게 하기 어렵기 때문인데, 그러면 시그니처가 모바일에서 사라진다. 쟁점 그룹 헤더와 `side` 라벨이 구조를 대신 잡아주지만 완전한 해법은 아니다. 화면 작업 시 재검토한다.
